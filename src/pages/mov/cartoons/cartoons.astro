---
import Layout from "../../../layouts/LayoutPage.astro";
import Cartoons2 from "./cartoons2.astro";

const response = await fetch("http://192.168.0.94:8888/intCartoons");
const data = await response.json();

---

<Cartoons2 />

<Layout title="Cartoons">
    <main>
        <div class="grid grid-cols-6 gap-2 flex items-center">
            {
                data.map((d) => (
                    <div class="m-4">
                        <img
                            id={d.movname}
                            src={d.httpthumbpath}
                            class="max-w-full h-auto rounded-lg"
                            alt={d.movname}
                        />
                    </div>
                ))
            }
        </div>
    </main>
</Layout>
