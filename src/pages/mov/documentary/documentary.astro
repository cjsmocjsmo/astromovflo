---
import Layout from "../../../layouts/LayoutPage.astro";
import Documentary2 from './documentary2.astro';

const response = await fetch("http://192.168.0.94:8888/intDocumentary");
const data = await response.json();

---

<Documentary2 />

<Layout title="Documentary">
    <main>
        <div class="grid grid-cols-5 gap-2 flex items-center">
            {
                data.map((d) => (
                    <div class="m-4">
                        <img
                            id={d.movname}
                            src={d.httpthumbpath}
                            class="max-w-full h-auto rounded-lg"
                            alt={d.movname}
                        />
                    </div>
                ))
            }
        </div>
    </main>
</Layout>
