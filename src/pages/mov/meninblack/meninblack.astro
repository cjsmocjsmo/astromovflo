---
import Layout from "../../../layouts/LayoutPage.astro";
import Meninblack2 from "./meninblack2.astro";

const response = await fetch("http://192.168.0.94:8888/intMenInBlack");
const data = await response.json();

---

<Meninblack2 />

<Layout title="Men In Black">
    <main>
        <div class="grid grid-cols-5 gap-2 flex items-center">
            {
                data.map((d) => (
                    <div class="m-4">
                        <img
                            id={d.movname}
                            src={d.httpthumbpath}
                            class="max-w-full h-auto rounded-lg"
                            alt={d.movname}
                        />
                    </div>
                ))
            }
        </div>
    </main>
</Layout>
