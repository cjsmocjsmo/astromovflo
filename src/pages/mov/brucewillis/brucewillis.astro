---
import Layout from "../../../layouts/LayoutPage.astro";
import BruceWillis2 from "./brucewillis2.astro";

const response = await fetch("http://192.168.0.94:8888/intBruceWillis");
const data = await response.json();
---

<BruceWillis2 />

<Layout title="Bruce Willis">
    <main>
        <div class="grid grid-cols-4 gap-2 flex items-center">
            {
                data.map((d) => (
                    <div class="m-4">
                        <img
                            id={d.movname}
                            src={d.httpthumbpath}
                            class="max-w-full h-auto rounded-lg"
                            alt={d.movname}
                        />
                    </div>
                ))
            }
        </div>
    </main>
</Layout>
