---
import Layout from "../../../layouts/LayoutPage.astro";
import Misc2 from "./misc2.astro";

const response = await fetch("http://192.168.0.94:8888/intMisc");
const data = await response.json();

---

<Misc2 />

<Layout title="Misc">
    <main>
        <div class="grid grid-cols-5 gap-2 flex items-center">
            {
                data.map((d) => (
                    <div class="m-4">
                        <img
                            id={d.movname}
                            src={d.httpthumbpath}
                            class="max-w-full h-auto rounded-lg"
                            alt={d.movname}
                        />
                    </div>
                ))
            }
        </div>
    </main>
</Layout>
