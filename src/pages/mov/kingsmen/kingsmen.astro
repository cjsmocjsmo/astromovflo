---
import Layout from "../../../layouts/LayoutPage.astro";
import Kingsmen2 from "./kingsmen2.astro";

const response = await fetch("http://192.168.0.94:8888/intKingsMan");
const data = await response.json();

---

<Kingsmen2 />

<Layout title="Kingsman">
    <main>
        <div class="grid grid-cols-3 gap-2 flex items-center">
            {
                data.map((d) => (
                    <div class="m-4">
                        <img
                            id={d.movname}
                            src={d.httpthumbpath}
                            class="max-w-full h-auto rounded-lg"
                            alt={d.movname}
                        />
                    </div>
                ))
            }
        </div>
    </main>
</Layout>
